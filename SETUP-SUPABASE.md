# دليل تشغيل المعرض والحسابات (Supabase + Netlify)

هذا الدليل لك خطوة بخطوة. لا تحتاج أي خبرة برمجية. الوقت المتوقع: 20–30 دقيقة.

> 💡 حتى تنتهي من هذه الخطوات، التطبيق يعمل عادياً: كل شيء محفوظ في جهاز المستخدم، والمعرض يظهر فيه "سيفتح قريباً".

---

## الجزء 1: إنشاء قاعدة البيانات في Supabase (مجاناً)

1. افتح **https://supabase.com** واضغط **Start your project**.
2. سجّل الدخول بحساب **GitHub** (نفس الحساب الذي فيه المشروع).
3. اضغط **New project** واملأ:
   - **Name:** مثلاً `alwan-sahla`
   - **Database Password:** اضغط **Generate a password**، ثم **احفظها في مكان آمن**.
   - **Region:** اختر أقرب منطقة لجمهورك.
   - **Plan:** Free
4. اضغط **Create new project** وانتظر دقيقتين حتى يجهز المشروع.

### إنشاء الجداول
5. من القائمة الجانبية افتح **SQL Editor**، ثم اضغط **New query**.
6. افتح ملف **`supabase/schema.sql`** من المشروع على GitHub، وانسخ **كل** محتواه.
7. الصقه في Supabase واضغط **Run**.
8. يجب أن تظهر رسالة **Success**. ✅ (إذا شغّلته مرة ثانية بالخطأ فلا مشكلة، فهو لا يحذف شيئاً.)

### إعداد تسجيل الدخول بالبريد
9. افتح **Authentication** ← **Sign In / Providers**، وتأكد أن **Email** مفعّل (يكون مفعّلاً عادةً).

---

## الجزء 2: نشر الموقع على Netlify (مجاناً)

1. افتح **https://app.netlify.com** وسجّل الدخول بحساب **GitHub**.
2. اضغط **Add new site** ← **Import an existing project** ← **GitHub**.
3. اختر مستودع **color-app**.
4. في الإعدادات:
   - **Branch to deploy:** `main`
   - **Build command:** اتركه فارغاً
   - **Publish directory:** اكتب `.` (نقطة فقط)
5. اضغط **Deploy**. بعد دقيقة يعطيك رابطاً مثل `https://xxxx.netlify.app`.
6. (اختياري) لتغيير الاسم: **Site configuration** ← **Change site name**، مثلاً `alwan-sahla` ← يصبح الرابط `https://alwan-sahla.netlify.app`.

> ✨ ميزة: كلما رفعت أنا تعديلاً على GitHub، يتحدث الموقع تلقائياً.

---

## الجزء 3: ربط الموقع بقاعدة البيانات

### أ) أخبر Supabase برابط موقعك
1. في Supabase افتح **Authentication** ← **URL Configuration**.
2. في **Site URL** اكتب رابط موقعك من Netlify، مثل `https://alwan-sahla.netlify.app`.
3. في **Redirect URLs** اضغط **Add URL** وأضف نفس الرابط.
4. اضغط **Save**.

(هذا مهم: بدونه، رابط الدخول في البريد لن يرجع المستخدم إلى موقعك.)

### ب) انسخ مفتاحي الاتصال
1. في Supabase افتح **Project Settings** (رمز الترس) ← **API** (أو **API Keys**).
2. انسخ:
   - **Project URL** (يشبه `https://abcdefgh.supabase.co`)
   - المفتاح العام: **anon public** أو **Publishable key**
3. ⚠️ **لا تنسخ أبداً** المفتاح السري **service_role / secret**. هذا يفتح كل شيء لأي شخص.

### ج) ضع المفتاحين في التطبيق
**الطريقة الأسهل:** أرسل لي القيمتين (Project URL والمفتاح العام) وأنا أضعهما.
المفتاح العام مسموح أن يكون ظاهراً، لأن قواعد الأمان في قاعدة البيانات هي التي تحمي البيانات.

**أو بنفسك على GitHub:**
1. افتح ملف `js/config.js` في المستودع (على الفرع `main`).
2. اضغط أيقونة القلم ✏️ (Edit).
3. ضع القيمتين بين علامات التنصيص:
   ```js
   const SUPABASE_URL = 'https://abcdefgh.supabase.co';
   const SUPABASE_ANON_KEY = 'eyJhbGciOi...';
   ```
4. اضغط **Commit changes**. سيتحدث موقع Netlify خلال دقيقة.

---

## الجزء 4: جرّب!
افتح موقعك من Netlify:
1. افتح أي لوحة ← اضغط **📢 انشر في المعرض** ← اكتب اسمك وبريدك ← **أرسل لي الرابط**.
2. افتح بريدك واضغط الرابط ← ترجع للموقع مسجّلاً ← اضغط **انشر في المعرض** مرة أخرى.
3. افتح **🖼 المعرض** من الأعلى: يجب أن ترى لوحتك، ويمكنك الإعجاب بها ♥.

---

## كيف أقرأ آراء المستخدمين؟
في Supabase افتح **Table Editor** ← اختر **feedback_readable**.
ستجد كل الإجابات مكتوبة بالعربي: التجربة، المشكلة، وما يساعد أكثر.

---

## ملاحظات مهمة
- **رسائل البريد:** خدمة البريد المدمجة في Supabase للتجربة فقط، وترسل عدداً **قليلاً جداً** من الرسائل في الساعة.
  قبل الإطلاق الحقيقي نربط خدمة بريد مجانية (مثل Resend). أخبرني عندما تصل لهذه المرحلة وأساعدك.
- **الخطة المجانية** في Supabase **تتوقف مؤقتاً** إذا لم يزر أحد الموقع أسبوعاً كاملاً.
  يصلك بريد، وتعيد تشغيلها بضغطة **Restore** من لوحة التحكم.
- **(اختياري) رسالة الدخول بالعربي:** Authentication ← Emails ← **Magic Link**، ويمكنك تغيير النص.
- **التكلفة:** كل ما سبق مجاني. تحتاج الدفع فقط إذا كبر التطبيق كثيراً (Supabase Pro: 25$ شهرياً)،
  أو إذا أردت اسم نطاق خاص مثل `alwan.app` (حوالي 10$–15$ سنوياً). تأكد من الأسعار في مواقعهم قبل الدفع.
